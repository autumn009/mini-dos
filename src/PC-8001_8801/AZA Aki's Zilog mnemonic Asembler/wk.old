	CALL	CMPLBL		;IF M(LOOPCNT)>M(DST)
	JR	Z,SRTNOT	;THEN SWAP ; FALG:=TRUE
	CALL	SWAPLBL
